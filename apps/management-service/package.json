{
	"name": "management-service",
	"version": "1.0.0",
	"scripts": {
		"build:deps": "pnpm exec tsc --noEmit",
		"build": "pnpm build:deps && tsup",
		"dev": "pnpm build:deps && tsx watch src/index.ts"
	},
	"private": true,
	"license": "UNLICENSED",
	"devDependencies": {
		"@types/js-yaml": "^4.0.9",
		"@types/node": "^22.13.9",
		"tsup": "^6.2.1",
		"tsx": "^4.7.1",
		"typescript": "^5.5.3"
	},
	"dependencies": {
		"@fastify/error": "^4.0.0",
		"fastify": "^5.2.1",
		"js-yaml": "^4.1.0"
	}
}